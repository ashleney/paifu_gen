{
    "name": "paifu_gen-www",
    "version": "0.1.0",
    "private": true,
    "scripts": {
        "build:wasm": "npx wasm-pack build --target web --out-dir ./www/pkg --out-name paifu_gen",
        "start": "npx http-server ./www -c-1 -p 8080",
        "dev": "npm run build:wasm && npm run start"
    },
    "devDependencies": {}
}